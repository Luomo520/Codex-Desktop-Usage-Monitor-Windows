# Changelog

## 3.1.3 - 2026-09-25

- 新增 CC Switch 当前 Codex 供应商联动，只读复用余额脚本、Token 汇总与最近请求信息；切换供应商后自动跟随，不复制凭据或修改 CC Switch 数据库。
- “本会话”新增“最近回答缓存”，按完成时间从新到旧显示最近 X 次已完成回答的缓存命中率；X 默认为 5，可设置为 1–20 并持久化。
- 折叠栏中的“近期缓存”改为显示最近 X 次有效缓存命中率的算术平均值，不再显示回答条数；展开面板继续显示逐次明细。
- 折叠监视栏优先移动到当前会话标题栏右侧，展开面板改为从标题栏下方展开；无法识别标题栏时保留输入框工具栏回退定位。
- 新增缓存低命中率标红阈值，默认 90%，可设为 0–100；总缓存、上次回答、近期平均和近期逐次明细严格低于阈值时标红，0 表示关闭。
- CC Switch 栏的“今日 Token”固定改用百万 Token 的 `M` 单位显示，不再使用“万”。
- 最近回答缓存与“上次回答缓存命中率”共用日志合并和去重口径：未完成回答不进入列表，跨恢复日志、重叠日志和重启重扫不会重复；缺少有效输入 Token 时显示 `--`。
- 补充中英文界面、设置归一化、重启恢复、日志恢复和界面生命周期测试。

## 3.1.2 - 2026-09-23

- 会话日志扫描复用目录清单，当前任务及当日目录有新文件时立即发现，每 30 秒完整核对其他历史目录；兼容 Windows 目录时间戳未更新的情况。
- 已知日志仍按原频率读取新增内容，保留当前任务、后台续跑任务及跨文件 Token 统计。

## 3.1.1 - 2026-09-20

- 设置新增“30 秒刷新”开关：官方订阅和 API 用量可在 30 秒与默认 60 秒之间切换，即时生效并持久化。
- 切换时替换后台轮询定时器并同步倒计时圆环，保留本地 Token 扫描和重置预测缓存频率。

## 3.1.0 - 2026-09-15

- 修复目标／计划模式控件挤占权限按钮后方空间时监视栏消失：自动向右寻找同一工具栏的可用间隙，退出模式后恢复原位置。

- 修复恢复日志以运行实例 ID 结尾时会话 Token 停滞：统一按日志头任务 ID 合并，按时间排序去重快照，累计 Token、总缓存命中率和上一轮回答统计跨文件连续更新。

- 新增“额度对应 Token”：独立持久化实时额度与本机官方订阅 Token 观测，按剩余额度每 10 点统计，并在有效跨度达到 3 点后推算 100% 周额度 Token；展示实测跨度、估计区间及本机覆盖限制。
- “额度对应 Token”内部改为两列布局，设置新增独立显隐开关，默认开启并持久化；隐藏时继续后台采集，保留观测记录和已选指标。
- 不重建历史额度，跨区间样本仅用于总体换算；超过 5 分钟的采集中断、重置和额度回升切断样本，接口异常时保留旧统计并标记过期。
- “缓存命中率”改为“总缓存命中率”，其下新增“上次回答缓存命中率”，在回答完成后更新。
- 展开栏排序调整为本会话、官方订阅、重置概率预测、额度对应 Token、API 账户、API Key；保留既有勾选与 API／预测显隐设置。
- 修正文档中的今日 Token 口径：累计快照变化后累加对应 last_token_usage，不是累计快照相减。

## 3.0.8 - 2026-09-11

- Pro 账户保留“5小时剩余”及勾选框，数值显示为 `--`，展开面板、普通和极简监视栏保持一致；重置时间使用主额度的 7 天窗口。官方订阅展示不再用 Spark 等模型独立额度补齐主额度窗口，后台原始额度和续跑判断不变。

## 3.0.7 - 2026-09-10

- 修复 Codex 加载较慢时误停监视器：启动端口等待延长至 180 秒，页面尚未就绪时保留后台并继续重试，重复启动复用等待中的进程。
- 启动验证改为只读检查指定后台进程的心跳，不再重新注入面板或把残留界面当作启动成功；替换后台确认连接后才停止旧进程。
- 修复启动日志为空时的空值异常，保留真实退出诊断；后台心跳中断时显示文字断连提示，明确标注旧数据和重新连接方法。
- “本会话”中的“当前状态”替换为“执行总耗时”，累计每轮正式耗时、执行中估算并在完成后校准；支持中英文、折叠栏和极简模式，标注估算及不完整记录。
- 原状态项勾选及排列顺序自动迁移；后台续跑状态判断保持不变。
- 耗时合并恢复后的多份会话日志，排除分叉继承和重复回合；兼容主动暂停的正式耗时，断流时冻结估算。

## 3.0.6 - 2026-09-10

- ChatGPT 普通聊天模式不挂载监视栏，ChatGPT Work 工作模式及 Codex 任务继续挂载；切换模式后自动恢复，保留原有设置。

## 3.0.5 - 2026-09-09

- 修复续跑只能记录一个等待任务、后台对话漏触发、额度耗尽后开启无效等问题；按对话持久化等待及去重记录，兼容旧记录。
- 官方额度在耗尽事件之后确认恢复即可续跑，支持提前恢复；暂时缺少会话数据时保留等待，确认有新的执行、完成或主动暂停时取消。
- 展示发送通道不可用、超时和发送失败原因；延长发送等待时间，同一渲染器内合并重复发送请求，原生排队消息变化不再把已创建回合误判为失败。

## 3.0.4 - 2026-09-05

- 续跑发送内容新增“为所有对话设置发送内容”开关，默认关闭；开启后共用发送内容，关闭后保留当时的统一内容并允许各自修改，不恢复旧内容；续跑启停仍按对话独立保存。

## 3.0.3 - 2026-09-05

- 重置概率预测栏新增“预告方式”，区分发放重置卡和直接重置；无有效明确预告时显示暂未明确，支持中英文，仅在展开面板展示。
- 完善 README 的安装、使用和数据来源说明，补充重置方式解释；新增小红书封面及生成提示词，作为仓库宣传素材保存，不进入运行安装包。

## 3.0.2 - 2026-09-01

- 在“重置概率预测（仅供参考）”栏底部新增 Tibo（`@thsottiaux`）最新 X 动态缩略卡片，显示正文摘要、发布时间和原帖链接；复用现有公开预测接口，并与概率数据同步每 5 分钟采纳更新。

## 3.0.1 - 2026-09-01

- 恢复 Codex 内嵌展开面板，并为每个数据源设置不随其他栏显隐变化的独立宽度：本会话、官方订阅和 API 账户各 230 px、API Key 170 px、重置概率预测 160 px。外框使用内容自适应宽度，自动计入边框、内边距和滚动条预留，避免最右栏被裁切。
- 收紧折叠监视栏“续跑”开关的额外占位，使右侧分隔线靠近开关；展开面板整体向左微移 16 px。

## 3.0.0 - 2026-09-01

- 将“额度恢复续跑”的开关和发送内容改为按 Codex 任务 ID 独立保存；在一个任务中启停或修改内容，不再影响其他任务。
- 自动续跑在额度恢复时直接向原任务提交续跑回合，原生排队消息保持原顺序和内容，不会被续跑消息覆盖或删除。
- 新增可关闭的“重置概率预测”栏，每 5 分钟读取 Codex Reset Observatory 的社区统计，显示未来 12/24/48/72 小时概率；该数据仅供展示，不参与额度恢复判断。
- 将“官方订阅”栏底部的版本与署名文字向左微调 8 px。
- 展开面板按原有栏宽向左扩展，新增栏位不再压窄其他栏；定位时优先保留完整右边缘，多出的宽度允许从左侧超出 Codex 内容区，不显示横向滚动条。设置开关固定为两列三行。
- 修复极简模式仍显示“续跑”文字的问题；折叠监视栏中的额度恢复续跑现在只保留可操作开关。
- 折叠监视栏改为严格按指标的勾选先后排序并持久化；取消后再次勾选的指标会移动到末尾。
- 将全局选项和品牌信息移到“官方订阅”栏底部，并收进“设置”折叠入口；新增 API 栏显示开关，新安装默认隐藏 API 账户与 API Key 两栏，已有设置保持原布局。
- “近7天 Token”改为与累计 Token 相同的官方基线加本机临时增量：官方值不变时继续累加，官方值变化时立即采用新值并清空临时增量；本机 Token 账本升级至 schema 8。
- 优化自动续跑开关的视觉样式：展开面板与折叠监视栏统一使用更清晰的开启色、滑块阴影、悬停和键盘焦点反馈，并兼容系统“减少动态效果”设置；修复轨道保留行内布局导致宽高失效、实际显示成竖线的问题。
- 自动续跑改用与 cc-connect Desktop CDP 模式相同的内部请求链路：在 Codex Desktop 渲染器中按明确会话 ID 调用 `thread/resume` 和 `turn/start`，不再填写或占用可见输入框；每个额度事件使用稳定的客户端消息 ID 防止重复提交，并保留额度恢复和失败重试保护。
- “本会话”新增“当前状态”，根据结构化会话事件显示“正在执行 / 执行完成 / 额度用尽暂停 / 主动暂停”，不依赖界面文字识别。
- 将“本次任务相关”改为独立的“本会话”栏并移到官方订阅左侧；“当前任务累计 Token”和“上次对话消耗 Token”分别改名为“当前会话累计 Token”和“上次回答消耗 Token”。
- 新增“自动压缩上下文次数”，按当前会话日志中唯一的 `compacted.payload.window_number` 计数，不重复计算配对的 `context_compacted` 通知。
- 展开面板改为多数据源栏布局；旧版显示项会把原官方订阅中的会话指标迁移到“本会话”栏。
- 修复“当前会话累计 Token”把 `total_token_usage` 的小幅回调误判成完整计数器重置、导致累计值成倍放大的问题；改为仅在累计快照变化时加入对应的 `last_token_usage`，相同快照不重复计算。
- 修复“上次回答消耗 Token”在当前回答仍生成时被当前轮实时数值覆盖的问题；现在只在对应 `task_complete` 到达后更新，生成期间保留最近一次已完成回答。
- 新增默认关闭的“额度恢复续跑”：与“当前状态”一起移到“本会话”栏底部；左侧复选框控制是否在折叠监视栏显示可直接操作的续跑开关，展开和折叠开关控制同一功能且不会误触面板。其下增加可持久化的发送内容输入框，默认“继续”，支持最多 500 字符的非空单行文本。任务因官方额度耗尽而结束时，等待对应官方周期确认恢复后，通过 Codex Desktop 内部请求客户端向原任务自动发送配置内容。
- “本会话”新增缓存命中率，按累计 `cached_input_tokens / input_tokens` 计算并兼容内部计数回退；“官方订阅”新增近7天 Token，汇总官方逐日用量桶中今天及前六个本机自然日。两项默认不在折叠栏显示。

## 2.1.5 - 2026-08-28

- 在展开面板的“5小时剩余”和“7天剩余”中分别显示“重置时间 · 剩余百分数”；普通及极简监视栏继续只显示剩余百分数。
- 合并 Microsoft Store Codex 在 PowerShell 7 下的安装发现修复，并更新应用内版本、安装包元数据、README 和展开面板截图。

## 2.1.4 - 2026-08-21

- 修复打开快速聊天或右侧栏“侧边聊天”时，将 `chatgpt:` 会话、侧边任务 UUID 或 `quick-chat-prewarm` 辅助渲染页误识别为当前 Codex 任务的问题；任务标识现在跟随监视栏所在的主 Composer，侧边栏过渡期间保留最后一个有效主任务标识。

## 2.1.3 - 2026-08-18

- 移除 Codex 主窗口失焦时自动隐藏监视栏的行为；点击其他窗口后监视栏继续显示。

## 2.1.2 - 2026-08-18

- 修复打开快速聊天或其他浮窗后监视栏仍在失焦的 Codex 主窗口绘制、看起来飘到浮窗输入区的问题；主窗口失焦时自动隐藏，重新聚焦后恢复原 Composer 锚点。
- 修复快速聊天重建主 Composer 后按 DOM 顺序误选快速聊天输入框的问题；优先识别主 Composer 的权限控制，重建后保持监视栏留在主编辑区。

## 2.1.1 - 2026-08-14

- 将“版本更新提醒 / Update alerts”改为“自动更新 / Auto update”；开启后由监视器后台每 24 小时检查一次 GitHub Release。
- 新增正式版本、精确资产名、下载主机、大小、ZIP 路径与 SHA-256 多层校验；通过后安装新版并热替换监视器后台，失败时保留当前版本且不重启 Codex。
- 移除渲染页面内的 GitHub 更新请求和版本链接逻辑，新增自动更新单元测试与发布包安全契约检查。
- 重构 README 首屏和快速安装流程，将非官方声明提前，并把详细指标口径与排障步骤拆分为独立文档。
- 使用隔离演示数据更新 v2.1.0 普通、极简、展开和 API Key 配置截图，不包含真实账号、额度、接口地址或凭据。
- 在完整说明中将 CCTQ 调整为 API 账户协议兼容性说明，API Key 配置继续保持供应商无关。

## 2.1.0 - 2026-08-11

- 将显示项选择和四个界面开关持久化到监视器自己的 `%LOCALAPPDATA%\CodexUsageMonitor\ui-settings.json`；Codex 重启、更新或重建页面存储后仍可恢复，原有页面设置会自动迁移。
- 全新设置的默认显示项改为仅“7天剩余”和“当前任务累计 Token”。
- 新增设置白名单校验、原子文件写入、后台重启恢复和页面同步回归测试；设置文件不保存账号凭据。
- API 账户和 API Key 栏标题新增“配置”按钮，可在展开面板内填写凭据与连接信息；凭据通过本机桥接验证并使用 Windows DPAPI 加密保存，成功后自动热重载监视器。
- 配置表单增加可见标签、密码显隐、内联校验、键盘焦点、保存中与成功/失败状态；密钥不回填，留空可保留已保存凭据。
- 新增面板配置安全回归测试，覆盖 Provider 校验、DPAPI 往返、明文落盘防护和无效账户请求拒绝。
- API Key 配置移除供应商下拉框；已有连接时默认只显示 Key，首次配置时显示服务地址与用量接口路径，其余字段折叠进高级设置。
- API 账户面板新增“累计 Token 基准”，保存时同步读取当前日志并建立去重检查点；不知道精确值时可填写 `0`。
- 更新应用内版本、安装包元数据、README 和测试至 v2.1.0。

## 2.0.2 - 2026-08-07

- 适配新版 Codex Desktop Composer 布局：识别 `ComposerLayoutRoot`，在审批按钮不存在时自动使用底部控件之间的最大可用空隙，避免监视栏因可用宽度误判为 0 而消失。
- 保留旧版审批按钮定位策略，兼容新旧 Composer 界面。
- 修复渲染页面初始化早期 `documentElement` 尚不可用时 MutationObserver 启动失败的问题。
- 新增新版 Composer 结构、最大空隙定位和初始化保护回归测试。
- 更新应用内版本、安装包元数据、README 和测试至 v2.0.2。

## 2.0.1 - 2026-08-05

- 将展开面板右下角四个显示开关改为两列等宽布局，并统一标签和开关轨道对齐。
- 将中文开关文案“版本提醒”改为“版本更新提醒”。
- English UI 的 Token 数值改用常见的 `K`、`M`、`B` 缩写；中文界面继续使用“万”“亿”。
- 更新应用内版本、安装包元数据、README、测试和 Release 清单至 v2.0.1。

## 2.0.0 - 2026-08-05

- 将界面常量、中英文文案、Codex DOM 定位和版本检查拆分为独立运行模块，发布包仍保持透明、可审计的脚本结构。
- 新增布局策略、定位失败原因、可用宽度和锚点状态自检；注入验证结果会返回具体策略与降级原因。
- 新增中文与 English UI 切换，覆盖数据源、指标、状态、任务分区、显示设置和折叠栏文案。
- 新增默认关闭的版本提醒；启用后最多每 24 小时无凭据查询一次 GitHub Releases API，仅显示发布页链接，不自动下载或执行更新。
- 本机任务日志继续按文件偏移量增量读取；任务活跃时约 2 秒扫描一次，空闲后降至约 12 秒，减少长期后台目录扫描。
- 页面隐藏时暂停布局兜底和倒计时刷新；布局兜底由 4 秒放宽至 20 秒，倒计时由 250 毫秒调整为 1 秒。
- 移除 Dream Skin DOM、全局变量、环境变量和旧状态目录兼容代码；v1 监视器显示设置会一次性迁移到 v2 设置键。
- 更新应用内版本、安装包元数据、README、测试和 Release 清单至 v2.0.0。

## 1.8.7 - 2026-08-05

- 适配新版 Codex Desktop 输入框新增的滚动容器布局：监视器宿主改为页面顶层固定定位，展开面板不再被输入框裁切，也不会撑大输入框并触发横向滚动条。
- 新增可滚动输入框祖先的界面生命周期回归测试，并更新 README 展开面板截图。

## 1.8.6 - 2026-08-02

- 修复正式 Release 包缺少 `.github` 目录时内置测试误读仓库专用工作流而中断的问题；源码检出仍严格校验发布工作流，并增加从构建产物直接运行包内自测的回归覆盖。

## 1.8.5 - 2026-08-02

- 修复 Codex 更新后桌面快捷方式变成空白或黑块图标的问题；安装时从本机 Codex 的透明 PNG 生成标准 ICO 缓存，不再引用带版本号的 WindowsApps 路径，也不再使用透明遮罩不稳定的可执行文件图标提取。
- 修复“本次任务相关”在任务 ID 或 Token 暂时不可用时消失的问题；数据缺失时保留分区并显示 `--`。
- 增强渲染器重载恢复与任务 ID 识别，监视进程等待渲染器恢复的宽限期由 15 秒延长至 60 秒。
- 修复重启验证超时时对空错误日志调用方法而遮蔽真实错误的问题；单次验证探针失败会继续重试，启动验证宽限期延长至 45 秒。
- 加固携带凭据的 API 请求：远程地址强制 HTTPS、仅本机回环地址允许 HTTP、拒绝重定向、流式限制响应体大小，并校验令牌与用户 ID。
- API 账户历史日志改为按需顺序补页，覆盖检查点后立即停止，降低并发请求和限流风险。
- 加固 CDP WebSocket、发布清单和依赖锁文件供应链校验，并补充对应回归测试。

## 1.8.4 - 2026-07-30

- Codex 在“请求批准”“替我审批”“完全访问权限”“自定义”之间切换时，监视栏会自动恢复并保持可见。
- 官方重置时间改为显示具体月日和分钟；折叠栏保留日期与时间之间的空格。
- 启动器优先使用独立安装的 Codex CLI，避免不兼容的桌面捆绑 CLI 持续触发官方额度查询超时；用户显式设置的 `CODEX_USAGE_CODEX_PATH` 仍具有最高优先级。
- 增强 Windows PowerShell 兼容性，补充启动器 CLI 选择、审批模式切换和界面布局回归测试，并固定本机 Token 测试的日期锚点，避免午夜边界随机失败。

## 1.8.2 - 2026-07-26

- “上次对话消耗 Token”改为按 `turn_context` 划分可见对话，并累计该轮全部 `token_count.total_token_usage` 正增量；工具调用导致的多次模型调用会全部计入，重复快照不会重复计算。
- 官方订阅“累计 Token”改为官方锚点加本机临时增量：官方完整整数不变时累计新产生的本机官方订阅 Token；官方值变化时清空临时增量并直接采用新官方值。账本升级至 schema 7，临时增量跨日期和重启持久化、去重。

## 1.8.1 - 2026-07-26

- 重写官方订阅“今日 Token”口径：读取本机 `token_count` 事件的 `total_token_usage` 正增量，直接累加原始 Token，不再按官方费率折算，也不再回退到官方当天日期桶。
- 每回合结合 `thread_settings_applied.model_provider_id` 与当前 ChatGPT 官方认证状态判断来源；仅官方订阅回合计入，API、API Key 和无法确认来源的回合均不计入。
- 对重复 Token 快照与 fork 重放事件去重；今日计数按本机日期持久化，在本地日期跨过零点时自动清零，Codex 或 Windows 当天重启后继续累加。

## 1.8.0 - 2026-07-26

- 官方订阅“今日 Token”增加本机实时计数：按 ChatGPT 认证提供方归属任务，并依照 OpenAI Codex 官方费率区分非缓存输入、缓存输入、输出、实际模型和 Fast 模式，统一显示为 GPT-5.6 Sol 标准输入等效 Token。
- 增加当前任务识别与本机日志增量扫描；“当前任务累计 Token”和“上次对话消耗 Token”可用于官方订阅、API Key 或第三方提供方任务，并独立放在“本次任务相关”分区。
- 官方实时计数按日期和认证提供方集合持久化、去重且不回退；官方当天日期桶延迟时仍可更新，Codex 或 Windows 重启后继续当天计数。
- 监视器改为渲染器出现后立即注入，不再等待首次账户请求；增强 CDP 端口发现、单次验证超时终止和辅助页面跳过逻辑，减少快捷方式启动等待与偶发监视框缺失。
- 展开面板调整底部控制区、任务分区和状态显示；更新 README 数据口径、配置来源说明、任务指标说明及新版界面截图。
- 增加官方费率折算、模型/Fast 模式识别、任务切换、持久化、启动超时与新分区的回归测试；发布包补齐极简模式截图。

## 1.7.2 - 2026-07-26

- 隐藏启动器增加 `-ExecutionPolicy Bypass`、入口校验和退出码捕获；PowerShell 脚本加载前失败也会写入 `launcher-error.log`。
- Microsoft Store 版 `codex.exe` 自动复制到当前用户状态目录运行，避免从受保护的 `WindowsApps` 直接启动时出现 `spawn EPERM`；Store 更新后自动刷新副本。
- 增加隐藏 VBS 参数/错误日志及 Store/非 Store CLI 路径回归测试。

## 1.7.1 - 2026-07-25

- 官方订阅、API 账户和 API Key 的统一刷新周期由 30 秒调整为 60 秒，倒计时表盘同步改为每 60 秒一圈。
- 修复 API 账户日志接口复用页内 `id` 导致累计与今日 Token 随机增减的问题；改用稳定请求 ID 或请求字段指纹去重。
- 累计与今日 Token 改为“时间检查点 + 同时间请求指纹”的单调追加账本，不再用不稳定日志行互相计算正负差值。
- 当最新一页无法覆盖上次检查点时并发补读后续分页；任一分页失败则保留原账本，避免残缺数据造成漏计或跳变。
- Token 状态升级到 schema v5；旧状态的累计值不会低于用户设置的初始基准，升级后建议重新执行一次基准配置。

## 1.7.0 - 2026-07-25

- 极简模式改为最多选择 14 项，并从 9 项开始启用双行压缩布局。
- 倒计时使用与文字同色的 13px 圆形表盘，顶部凸起为 3px，指针为 1.5px 标准绿色。
- 折叠监视栏字号跟随“替我审批”按钮，并修正极简模式下数据项的垂直对齐。
- API 账户累计与今日 Token 会保存每个日志 ID 上次读取的 Token 数，同一日志后续结算变化时按差值更新，避免漏算或重复。
- API 账户每轮固定刷新最新日志页，并同步轮询一个历史页，使后结算 Token 在不集中请求全部分页的情况下持续校准。

## 1.6.0 - 2026-07-25

- 展开面板增加“极简模式”滑动开关；开启后折叠监视栏只显示数值与单位，选中 7 项及以上时才启用压缩布局。
- 展开面板增加“倒计时可视化”滑动开关；开启后折叠监视栏最左侧显示淡绿色环形刷新进度。
- 折叠监视栏的密度字号只作用于折叠按钮，不再改变展开面板字体大小。
- 显示模式保存在 Codex 本地页面设置中，并增加开关持久化、6/7 项压缩边界、环形进度和布局回归测试。
- README 补充显示模式说明，并明确单实例与退避无法阻止第三方服务商自身返回 HTTP 429。

## 1.5.1 - 2026-07-24

- API Key 接口返回 HTTP 429 时保留上一次成功数据，显示“请求受限”，并以 60 秒至 5 分钟的退避间隔自动重试。
- 监视器改为全局单实例，启动时清理其他端口或旧版本残留的监视进程，避免重复请求触发服务商限流。
- 后台注入器连续 15 秒找不到 Codex 页面目标时自动退出，避免 Codex 关闭后继续请求用量接口。
- 修复跳过刷新时请求锁未释放、到达重试时间后无法继续刷新的问题。
- 调整 README 开篇预览、简要安装说明及 API Key 限流排查指引。

## 1.5.0 - 2026-07-24

- 统一官方订阅、API 账户和 API Key 的三栏用量面板，支持跨来源选择最多 8 项折叠显示。
- API 账户增加持久化凭据、分页缓存和累计 Token 初始值/去重累加。
- API 账户的今日 Token 按本机日期持久化并按日志 ID 去重，Codex 或 Windows 重启后保持计数，跨日自动归零。
- 刷新周期统一为 30 秒，并在请求失败时保留上一次成功数据。
- 重写 README：先提供小白快速安装，再说明每项数据、配置、安全边界和异常排查。
- 增加 Codex 安装时的 API 账户、API Key、响应格式适配和累计 Token 基线指引。
- 启动器在首选 CDP 端口被其他程序或残留监听占用时自动选择后续可用端口。
- 折叠栏在 5 项及以上启用紧凑显示，展开面板左下显示刷新信息、右下显示项目署名。
- README 增加使用脱敏演示数据截取的折叠栏与三栏面板实机截图。

## 1.4.0 - 2026-07-24

- 安装器会自动发现 Microsoft Store 和常见非 Store Codex Desktop 路径，并从桌面程序目录推导相邻的 `codex.exe`。
- 非 Store 路径发现成功后自动保存到当前用户环境；完全找不到时才询问用户输入真实可执行文件路径。
- 增加非 Store 自动发现测试和 Codex 安装指引。

## 1.3.1 - 2026-07-24

- 指示灯收敛为绿色、黄色、红色和灰色：成功为绿色、同步为黄色、旧数据为红色，请求失败与暂无数据统一为灰色。
- 同步更新指示灯说明与状态颜色测试。

## 1.3.0 - 2026-07-24

- 桌面快捷方式更名为 `Codex Usage Monitor`，并通过 Windows Script Host 隐藏启动 PowerShell，不再闪现命令行黑框。
- 指示灯使用绿色、蓝色、紫色、洋红色和灰色区分成功、同步、过期、失败和无数据状态。
- 增加启动错误日志、指示灯状态说明和面向不同 Windows 环境的运行异常排查指南。
- 增加隐藏启动器、快捷方式目标和状态颜色测试。

## 1.2.0 - 2026-07-23

- 增加面向 Codex 安装流程的根目录 `install.ps1`，将运行文件复制到当前用户的稳定版本目录后创建桌面快捷方式。
- 增加 `AGENTS.md`，为 Codex 提供安装、安全、API key 保护和测试指引。
- 使用统一的 `config/package-files.json` 驱动安装器、发布包和发布测试，减少文件清单漂移。

## 1.1.0 - 2026-07-23

- API key 默认使用当前 Windows 用户 DPAPI 加密持久化，Codex 或 Windows 重启后自动恢复。
- 增加 `-SessionOnly` 临时会话模式和 `clear-api-provider.ps1` 凭据清除命令。
- 增加 DPAPI 轮换、明文保护、自动导入和清除测试。

## 1.0.0 - 2026-07-23

- 将项目收敛为独立的 Windows Codex 用量监视器。
- 支持官方订阅的短周期余量、重置时间和 Token 统计。
- 支持声明式 API Provider、CCTQ 示例与动态来源名称。
- API key 改为仅在后台进程内存中使用。
- 新增 Provider 安全校验、renderer 生命周期测试、敏感信息扫描和发布包白名单。
- 数据刷新只更新现有面板，renderer 断开后可自动重新连接。
- API 状态接口失败时保留主用量接口的新数据与最近一次状态配置。
- 增加启动互斥、运行版本识别、非标准安装路径和 WindowsApps manifest 回退。
- 移除主题、图片素材、主题管理器、EXE 构建器和自动强制重启逻辑。
- 增加源码与运行包两种下载说明，以及 Windows CI 和自动 Release 构建流程。
